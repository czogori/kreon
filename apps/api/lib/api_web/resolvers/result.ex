defmodule ApiWeb.Resolvers.Result do
    # def all(args, _info) do
    #     {:ok, Kreon.LearningObject.Query.Results.execute(args)}
    # end

    # def my(args, _info) do
    #     {:ok, Kreon.LearningObject.Query.My.execute(args)}
    # end

    # def commit(args, _info) do
    #     {:ok, Kreon.LearningObject.Command.Commit.execute(args)}
    # end
end